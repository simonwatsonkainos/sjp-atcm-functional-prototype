Initial project to spike through considerations for ATCM.
Project is Spring Boot with Liquiase for DB schema change management.

Apply database updates by running the devRecreateDatabase.sh script in the /dev folder, this will also populate the DB with two test records.
Run application with go script at root of project.
